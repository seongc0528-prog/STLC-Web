NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Standard Web Push VAPID key pair (generate with: npx web-push generate-vapid-keys)
# Public key is safe to expose client-side; private key is an Edge Function secret only.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
